{
  "path": "/src/web/ssr.tsx",
  "declarations": [
    {
      "type": "function",
      "name": "streamHandler",
      "description": "A reference to the default stream handler for server-side rendering, used to handle streaming SSR responses in Tanstack React Start.",
      "parameters": {
        "type": "object",
        "description": "No parameters.",
        "properties": {}
      },
      "returns": {
        "type": "any",
        "description": "A stream handler function for SSR."
      },
      "signature": "const streamHandler = defaultStreamHandler;",
      "implementationNotes": "This is a direct assignment of the imported defaultStreamHandler. It is used as the stream handler for SSR in the Tanstack React Start server setup.",
      "dependencies": {
        "internal": [
          {
            "importPath": "src/web/router",
            "dependsOn": ["createRouter"]
          }
        ],
        "external": [
          {
            "name": "@tanstack/react-start/server",
            "importPath": "@tanstack/react-start/server",
            "dependsOn": ["defaultStreamHandler"]
          }
        ]
      }
    }
  ]
}
